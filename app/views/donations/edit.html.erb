   <%= simple_form_for [@project, @donation] do |f| %>
      <%= f.input :status %>
      <%= f.input :quantity_donated %>
      <%= f.submit "Add a Review", class: "btn btn-primary d-flex justify-content-center" %>
    <% end %>
